<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="CaptionText" parent="@android:style/TextAppearance.Material.Subhead">
        <item name="android:textColor">@color/black</item>
        <item name="android:fontFamily">sans-serif</item>
    </style>

    <style name="SmallText" parent="@android:style/TextAppearance.Small">
        <item name="android:textColor">@color/light_grey</item>
        <item name="android:fontFamily">sans-serif</item>
    </style>

    <style name="SmallPrimaryText" parent="@android:style/TextAppearance.Small">
        <item name="android:textColor">@color/colorPrimary</item>
        <item name="android:fontFamily">sans-serif</item>
    </style>
    <!--<style name="RedText" parent="@android:style/TextAppearance.Small">-->
        <!--<item name="android:textColor">@color/light_grey</item>-->
        <!--<item name="android:fontFamily">sans-serif</item>-->
    <!--</style>-->

    <!--<style name="LinearProgress" parent="Theme.AppCompat.Light">-->
        <!--<item name="colorAccent">@color/indigo</item>-->
        <!--<item name="android:progressBackgroundTint">@color/pink</item>-->
    <!--</style>-->
</resources>
